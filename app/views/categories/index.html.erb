<p id="notice"><%= notice %></p>

<h1>Categorias</h1>

<table class="table">
  <thead "thead-default">
    <tr>
      <th>Name</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @categories.each do |category| %>
      <tr>
        <td><%= category.name %></td>
        
        <td><%= link_to 'Editar', edit_category_path(category), class: 'glyphicon glyphicon-pencil' %></td>
        <td><%= link_to 'Exluir', category, method: :delete, data: { confirm: 'Você tem certeza?' }, class: "glyphicon glyphicon-trash"  %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Nova Categoria', new_category_path, class: 'btn btn-primary' %>
